import React from 'react';
import ReactDOM from 'react-dom';
import {Component_r14 as Component} from './component.jsx'

export const renderComponent = (parentElement) => {
  ReactDOM.render(<Component />,  parentElement);
}
